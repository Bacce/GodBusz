VITE_BACKEND_URL=http://localhost:3000

# Full URL template for the tile server.
# Use the default (relative path) when tiles are served by the same
# backend. Override with a CDN or external tile server for production.
# VITE_TILES_URL=/Tiles/{z}/{x}/{y}.png
